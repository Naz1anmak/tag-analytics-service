version: '3.7'

services:
  postgres:
    image: postgres:14
    container_name: tag-analytics-db
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123456
    ports:
      - "5433:5432"
